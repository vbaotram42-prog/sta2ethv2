# STA2ETH 项目改造说明

## 概述

本项目已从 eth2ap (以太网到WiFi AP) 成功转换为 sta2eth (WiFi STA到以太网) 方向。

## 主要改动

### 1. 项目配置

- **项目名称**: eth2ap → sta2eth
- **删除文件**: main/Kconfig.projbuild (所有Kconfig配置已删除)
- **WiFi凭证硬编码**:
  - SSID: "xinxin"
  - 密码: "19840226"
  - 认证方式: WPA2-PSK

### 2. 工作原理

```
[PC电脑] ←以太网→ [ESP32网桥] ←WiFi→ [AP路由器"xinxin"]
```

#### 数据包转发流程:

1. **以太网到WiFi** (pkt_eth2wifi):
   - PC发送的数据包通过以太网到达ESP32
   - ESP32直接转发到WiFi STA接口
   - 数据包发送到AP "xinxin"

2. **WiFi到以太网** (pkt_wifi2eth):
   - AP发来的数据包到达ESP32的WiFi STA接口
   - 数据包进入流控队列
   - 工作任务从队列取出并转发到以太网
   - PC通过以太网收到数据包

### 3. MAC地址处理方案

**重要**: ESP32使用WiFi STA的MAC地址作为以太网接口的MAC地址

```c
// WiFi连接到AP后:
esp_wifi_get_mac(WIFI_IF_STA, s_wifi_mac);
esp_eth_ioctl(s_eth_handle, ETH_CMD_S_MAC_ADDR, s_wifi_mac);
```

**优点**:
- AP路由器只看到一个设备(一个MAC地址)
- 数据包路由简单可靠
- 避免MAC地址冲突

### 4. DHCP配置分析

#### DHCP工作方式:

ESP32作为**二层网桥**(Layer 2 Bridge)工作:

1. **WiFi STA侧**:
   - ESP32作为DHCP客户端从AP获取IP地址
   - 触发事件: IP_EVENT_STA_GOT_IP
   
2. **以太网侧**:
   - **不运行DHCP服务器**
   - **不运行DHCP客户端**
   - 仅进行二层(MAC层)数据包转发

#### PC获取IP地址的过程:

```
1. PC发送DHCP DISCOVER → ESP32以太网接口
2. ESP32转发 → WiFi STA → AP路由器
3. AP的DHCP服务器响应 DHCP OFFER
4. ESP32转发响应 → 以太网 → PC
5. DHCP握手完成，PC获得IP地址
```

**结果**: ESP32和PC都从同一个DHCP服务器(AP "xinxin")获取IP地址

### 5. 配置位置

所有配置已从Kconfig移除并放入以下位置:

#### sdkconfig.defaults:
```
CONFIG_EXAMPLE_ETH_MDC_GPIO=31
CONFIG_EXAMPLE_ETH_MDIO_GPIO=52
CONFIG_EXAMPLE_ETH_PHY_RST_GPIO=51
CONFIG_EXAMPLE_WIFI_SSID="xinxin"
CONFIG_EXAMPLE_WIFI_PASSWORD="19840226"
```

#### 代码中硬编码:
- WiFi SSID: "xinxin"
- WiFi密码: "19840226"
- WiFi认证模式: WPA2-PSK
- 流控队列大小: 40
- 流控超时: 100ms

## 使用方法

### 编译和烧录

```bash
# 设置ESP-IDF环境
. $IDF_PATH/export.sh

# 编译
idf.py build

# 烧录并监控
idf.py -p /dev/ttyUSB0 flash monitor
```

### 预期行为

1. **WiFi连接**:
   ```
   Wi-Fi STA started, connecting to AP...
   Wi-Fi STA connected to AP
   Got IP Address: 192.168.x.x
   ```

2. **以太网连接**:
   ```
   Ethernet Started
   Ethernet Link Up
   ```

3. **数据包转发**:
   - PC通过以太网连接到ESP32
   - PC可以访问AP路由器网络
   - PC从AP的DHCP服务器获取IP地址
   - PC和ESP32在同一子网

## 技术细节

### 网络层次结构

```
第三层(IP层):   [PC] ←透明转发→ [AP"xinxin"]
                      ESP32从AP获取IP
                      
第二层(MAC层):  [PC] ←[ESP32以太网]←[ESP32 WiFi]→ [AP]
                      两个接口使用相同MAC地址
```

### 关键特性

1. **混杂模式**: 以太网接口设置为混杂模式接收所有数据包
2. **WiFi内部API**: 使用ESP-IDF内部API进行数据包直接转发
3. **流控机制**: 队列机制处理WiFi和以太网速度差异
4. **自动重连**: WiFi断开后自动重连到AP

## 故障排除

### WiFi无法连接
- 确认AP "xinxin" 存在且可用
- 检查密码 "19840226" 是否正确
- 确保AP在2.4GHz频段(ESP32不支持5GHz)

### 以太网不工作
- 检查以太网线连接
- 验证GPIO引脚(31, 52, 51)与硬件匹配
- 检查PHY复位GPIO是否正确

### PC无法获取IP
- 确保WiFi已先连接
- 检查AP的DHCP服务器已启用
- 查看日志是否有数据包发送失败的错误

## 文件清单

- `CMakeLists.txt` - 项目配置，项目名改为sta2eth
- `sdkconfig.defaults` - SDK默认配置，包含硬编码的WiFi和以太网设置
- `main/ethernet_example_main.c` - 主程序，实现sta2eth功能
- `main/CMakeLists.txt` - 主组件配置
- `main/idf_component.yml` - 组件依赖配置
- `README_sta2eth.md` - 英文详细文档
- `项目说明_中文.md` - 本文档

## 参考资料

- [ESP-IDF WiFi Station示例](https://github.com/espressif/esp-idf/tree/master/examples/wifi/getting_started/station)
- [ESP-IDF以太网示例](https://github.com/espressif/esp-idf/tree/master/examples/ethernet)
- [ESP-IDF sta2eth示例](https://github.com/espressif/esp-idf/tree/master/examples/network/sta2eth)
